import React from 'react';
import { Submission } from '../Pages/Submissions/Submissions';

type Props = {
    submission: Submission;
    key: string;
}

export const SubmissionRow = (props: Props) => {
    return null;
}